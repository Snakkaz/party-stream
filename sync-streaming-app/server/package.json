{
  "name": "sync-streaming-app",
  "version": "1.0.0",
  "description": "A synchronized streaming application that allows users to watch videos together in real-time.",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "build": "tsc",
    "dev": "ts-node-dev src/server.ts"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.0.0",
    "typescript": "^4.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.0.0",
    "ts-node-dev": "^1.0.0",
    "@types/express": "^4.17.0",
    "@types/socket.io": "^3.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}